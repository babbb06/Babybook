<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/buttonBackgroundColor"/> <!-- Button background color -->
    <corners android:radius="30dp"/> <!-- Corner radius -->
    <stroke android:width="2dp" android:color="@color/buttonBackgroundColor"/> <!-- Border color and width -->
</shape>
